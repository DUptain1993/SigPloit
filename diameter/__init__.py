# The Diameter modules use fully-qualified ``diameter.*`` imports (like the 5G
# package), so they do NOT put this directory on sys.path and their generic
# sub-package names never collide with the GTP package's top-level names.
